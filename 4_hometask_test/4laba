<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<p>1) Ответ 10 <br>
    При создании функция получает скрытое свойство [[Scope]], которое ссылается на лексическое окружение, в котором она была создана.
    В функции z нет переменной x поэтому с помощью scope функция находит переменную x во внешенем окружениии x=10
</p>
<p>
    2) Answer 30 <br>
    Функции z не надо идти во внешний scope поскольку она получает переменные a,b как аргументы поэтому эти аргументы(или свойства обекта Lexical enviroment function z)
</p>
<p>
    3)Answer 10,20<br>
    При таком вызове функцции параметры x,y равны аргументам a,b (неявная функция)
</p>
<p>
4)Answer 40<br>
    В function espression func хранится результат вызова function declaration Generate
</p>
<p>5) Выведется ошибка <br>
    Поскольку после последнего свойства оъекта двоетеочие не ставися <br>
   или<br>
    если убрать точку с запятой после field:x*y;<br>
function handle в резуьтате выполнеие возращает объект с одним свойством
</p>
<p>
    6)600<br>
    a=x=40(неявный вызов функции где фргумент x, параметр a),b=a=40(неявный вызов функции где фргумент a, параметр b),x=15(берется из ближайшего scope)
</p>
<p>
    7)Alex <br>
    name в конструкторе Human ,из свойств name которое задаетс  параметром name полученного врезультате вызова
    данного конструктора с аргументом "Alex"
</p>
<p>
    8) Maria
    Свойство объект Alex вызывается в контесте объекта Maria(если бы не было this in console.log(this.name)
    то вывело бы Alex ) this в this.name принудительно вызывает контест Maria
</p>
<p>
 9)Error
    call.apply (func arguments)- так работате поскольку call вызывает функцию а apply передает аргументы
</p>
<p>
10) 105
    [1,2,3].reverse.call(...) - необхадимо для вызова reverse c контестом [1,..10] ,после е%3 остается отсортированный из [1,..,10] все не делящиеся на 3 нацело, e*3 домножаем каждый элемент, reduce слаживает элементы и отнимает 1 где изначальные  p-24 c-30(поскольку массив был перевернут ), typeof res Number тк это xbckj
</p>
<script src="script1.js"></script>
</body>
</html>
